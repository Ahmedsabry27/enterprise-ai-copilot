import { useState } from "react";
import AuditTable from "../../components/audit/AuditTable";
import { ResourceState } from "../../components/common/ResourceState";
import { useAuditLogs } from "../../hooks/useAuditLogs";
export default function AuditPage(){const [filters,setFilters]=useState({action:"",agent:"",date:"7d",search:""});const {data,isLoading,error}=useAuditLogs(filters);const select=(key,options)=><select value={filters[key]} onChange={e=>setFilters({...filters,[key]:e.target.value})} className="rounded-lg border border-white/10 bg-white/5 px-3 py-2 text-sm text-slate-200"><option value="">All {key==="action"?"Actions":"Agents"}</option>{options.map(x=><option key={x}>{x}</option>)}</select>;return <main className="min-h-full bg-gradient-to-br from-[#071426] via-[#0B1F3A] to-[#142B52] p-5 text-white md:p-8"><header><h1 className="text-3xl font-bold">Audit Logs</h1><p className="mt-1 text-slate-400">Track AI system activities</p></header><div className="mt-7 flex flex-wrap gap-3">{select("action",[...new Set((data||[]).map(x=>x.action))])}{select("agent",[...new Set((data||[]).map(x=>x.agent))])}<select value={filters.date} onChange={e=>setFilters({...filters,date:e.target.value})} className="rounded-lg border border-white/10 bg-white/5 px-3 py-2 text-sm"><option value="7d">Last 7 days</option><option value="30d">Last 30 days</option></select><input placeholder="Search logs…" value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})} className="min-w-48 rounded-lg border border-white/10 bg-white/5 px-3 py-2 text-sm outline-none"/></div><div className="mt-5"><ResourceState loading={isLoading} error={error} empty={!data?.length} label="audit records"><AuditTable items={data}/></ResourceState></div></main>}
