import { useState } from "react";
import { Link } from "react-router-dom";
import { Plus, X } from "lucide-react";
import { ResourceState } from "../../components/common/ResourceState";
import WorkflowTable from "../../components/workflows/WorkflowTable";
import { useWorkflows } from "../../hooks/useWorkflows";
const input="mt-1 w-full rounded-lg border border-white/10 bg-slate-950/30 px-3 py-2 text-white outline-none focus:border-violet-400";
export default function WorkflowsPage(){const [open,setOpen]=useState(false),[form,setForm]=useState({name:"",description:"",assigned_agent:"",trigger_type:"MANUAL"});const {query,create,execute,remove}=useWorkflows();const submit=e=>{e.preventDefault();create.mutate(form,{onSuccess:()=>setOpen(false)});};return <main className="min-h-full bg-gradient-to-br from-[#071426] via-[#0B1F3A] to-[#142B52] p-5 text-white md:p-8"><header className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-3xl font-bold">Workflows</h1><p className="mt-1 text-slate-400">Manage your enterprise workflows</p></div><div className="flex gap-3"><Link to="/workflows/builder" className="inline-flex items-center rounded-lg border border-violet-400/40 bg-violet-500/15 px-4 py-2 text-sm font-medium text-violet-200">Workflow Builder</Link><button onClick={()=>setOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-gradient-to-r from-indigo-500 to-violet-500 px-4 py-2 text-sm font-medium"><Plus size={16}/>New Workflow</button></div></header><div className="mt-7"><ResourceState loading={query.isLoading} error={query.error} empty={!query.data?.length} label="workflows"><WorkflowTable workflows={query.data} onRun={id=>execute.mutate(id)} onDelete={id=>remove.mutate(id)}/></ResourceState></div>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/70 p-4"><form onSubmit={submit} className="w-full max-w-lg rounded-2xl border border-white/10 bg-[#102444] p-6 shadow-2xl"><div className="flex justify-between"><h2 className="text-xl font-semibold">Create Workflow</h2><button type="button" onClick={()=>setOpen(false)}><X/></button></div>{[["Workflow Name","name"],["Description","description"],["Assigned Agent","assigned_agent"],["Trigger Type (Manual / API / Scheduled)","trigger_type"]].map(([label,key])=><label key={key} className="mt-4 block text-sm text-slate-300">{label}<input required={key==="name"} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})} className={input}/></label>)}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={()=>setOpen(false)} className="rounded-lg px-4 py-2 text-slate-300">Cancel</button><button disabled={create.isPending} className="rounded-lg bg-violet-600 px-4 py-2 font-medium">{create.isPending?"Creating…":"Create Workflow"}</button></div></form></div>}</main>}
