import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts";
const colors={completed:'#22c55e',running:'#3b82f6',pending:'#f59e0b',failed:'#ef4444'};
export default function WorkflowDistribution({ distribution }) { const data=Object.entries(distribution).map(([name,value])=>({name,value})); const total=data.reduce((sum,item)=>sum+item.value,0); return <section className="rounded-2xl border border-white/10 bg-white/5 p-6"><h2 className="text-lg font-semibold">Workflow Distribution</h2><div className="mt-4 flex h-64 items-center"><ResponsiveContainer width="55%"><PieChart><Pie data={data} dataKey="value" innerRadius={58} outerRadius={84} paddingAngle={3}>{data.map(item=><Cell key={item.name} fill={colors[item.name]}/>)}</Pie><Tooltip contentStyle={{background:'#0b1f3a',border:'1px solid #ffffff1a',borderRadius:12}}/></PieChart></ResponsiveContainer><div className="space-y-3 text-sm">{data.map(item=><div key={item.name} className="flex gap-2 capitalize"><i className="mt-1 h-3 w-3 rounded-sm" style={{background:colors[item.name]}}/>{item.name} <span className="text-slate-400">{item.value}</span></div>)}<p className="pt-1 text-slate-400">{total} total</p></div></div></section>; }
