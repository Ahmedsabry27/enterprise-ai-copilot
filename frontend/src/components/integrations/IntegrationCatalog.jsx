import {Cloud,CheckCircle2,Clock3} from "lucide-react";
export default function IntegrationCatalog({items=[],onConfigure}){return <div className="integration-grid">{items.map(item=><article className="integration-card catalog" key={item.type}><div className="integration-title"><span className="tool-icon"><Cloud/></span><div><h2>{item.name}</h2><small>{item.category}</small></div></div><p>{item.description}</p><p className="catalog-auth">Auth: {(item.auth_methods||[]).join(", ").replaceAll("_"," ")}</p>{item.read_capabilities!=null&&<p>{item.read_capabilities} read · {item.write_capabilities} write capabilities</p>}<button className={item.implementation_status==="available"?"primary-button":"outline-button"} disabled={item.implementation_status!=="available"} onClick={()=>onConfigure(item)}>{item.implementation_status==="available"?<><CheckCircle2 size={14}/> Configure</>:<><Clock3 size={14}/> Coming soon</>}</button></article>)}</div>}
